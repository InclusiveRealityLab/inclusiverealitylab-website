// `name` is the value the sheet stores against each person; `label` is what the
// filter shows.
const peopleCategories = [
  { id: "lab", name: "Lab", label: "Lab members" },
  { id: "collaborator", name: "Collaborator", label: "Collaborators" },
  { id: "alumni", name: "Alumni", label: "Alumni" },
];

export default peopleCategories;
